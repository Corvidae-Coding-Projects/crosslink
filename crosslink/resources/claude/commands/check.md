---
allowed-tools: Bash(tmux *), Bash(docker *), Bash(podman *), Bash(crosslink *), Bash(git *), Read, Skill
description: Inspect running or completed kickoff agents
argument-hint: [agent]
---

Apply the `check` skill to $ARGUMENTS. Use Crosslink status and bounded logs, verify process and worktree evidence, and report the run’s actual state without sending input or cleaning it up.
