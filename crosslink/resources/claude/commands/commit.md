---
allowed-tools: Bash(git *), Bash(crosslink *), Skill
description: Commit the intended work and record its result
argument-hint: [message guidance]
---

Use the `commit` skill. Select only relevant paths, review verification evidence, create the commit with normal hooks, and add a result comment to the active Crosslink issue when present.

Message guidance: $ARGUMENTS
