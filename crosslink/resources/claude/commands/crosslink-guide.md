---
allowed-tools: Bash(crosslink *), Read, Grep, Skill
description: Answer a question about Crosslink commands or workflow
argument-hint: <question>
---

Use the `crosslink-guide` skill and current `crosslink --help` output to answer this question accurately:

$ARGUMENTS

The `.crosslink/rules/*.md` files are zero bytes; their prompt-hook loader remains wired.
