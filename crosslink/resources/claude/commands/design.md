---
allowed-tools: Bash(crosslink *), Bash(git *), Bash(gh *), Bash(ls *), Read, Grep, Glob, Write, Skill
description: Produce or revise a repository-grounded feature design
argument-hint: ["feature"] [--issue <id>] [--gh-issue <number>] [--continue <slug>]
---

Use the `design` skill with these arguments:

$ARGUMENTS

Explore the actual implementation and tests, resolve material product decisions, write the design under `.design/`, and validate its acceptance evidence.
