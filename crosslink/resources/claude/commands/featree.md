---
allowed-tools: Bash(git *), Bash(crosslink *), Bash(ls *), Bash(test *), Bash(mkdir *), Read, Skill
description: Create and initialize an isolated feature worktree
argument-hint: <feature description>
---

Use the `featree` skill for $ARGUMENTS. Preserve current work, create the linked worktree, install both provider integrations by default, and verify shared issue visibility.
